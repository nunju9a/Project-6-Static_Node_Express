extends layout.pug

block content 
  article.grid-container.portfolio-intro
    div

      //- My title
      h1 My Journey Through Javascript

      //- My description
      p.lead.text-light
        | This project showcases my knowledge of "server-side" Javascript by creating a portfolio of my current projects.
        | Linked below are some of the recent projects I have worked on during my time as a Developer Apprentice at Verizon.
        | Feel free to check them out!
  article.grid-container.portfolio-index
    
    .grid-x.grid-margin-x.small-up-2.medium-up-2.large-up-3

    
      
      //- Using each and interpolation to display each project 
      each project in projects
        .cell
          a(href=`/projects/${project.id}`)
            img.thumbnail(src= `${project.image_urls[0]}`)
            h5 #{project.project_name}